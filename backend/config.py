# config.py
# This file stores configuration settings for the backend.
# Example: Define database connection strings, environment variables, and constants.
# Use a library like python-decouple or dotenv to manage sensitive information.
# Ensure this file is not exposed in version control (e.g., add to .gitignore).

# Example configuration:
# DATABASE_URL = "sqlite:///example.db"
# SECRET_KEY = "your-secret-key"
